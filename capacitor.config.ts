import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.socialhealth.app",
  appName: "social-health",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
